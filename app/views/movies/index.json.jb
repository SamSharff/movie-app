@movies.map do |movie|
  {
    id: movie.id,
    title: movie.title,
    year: movie.year,
    plot: movie.plot,
    director: movie.director,
    # actors: movie.actors,
    # genres: movie.genres,
    english: movie.english
  }
end
